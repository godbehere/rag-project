// import PptxParser from 'pptx-parser';

// export async function parsePPTX(filePath: string): Promise<string> {
//   const slides = await PptxParser.parsePptx(filePath);
//   return slides.map((s: any) => s.text).join('\n');
// }